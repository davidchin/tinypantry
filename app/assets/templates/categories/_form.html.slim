form ng-submit="submit()"
  label Name
  input type="text" ng-model="category.data.name"

  label Keywords
  p ng-repeat="keyword in category.data.keywords"
    input type="text" placeholder="Keyword" ng-model="keyword.name"

  button type="submit" Submit
  button type="button" ng-show="category.data.id" ng-click="destroy()" Delete
