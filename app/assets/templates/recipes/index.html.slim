flash name="app"

section class="recipe-nav-bar"
  div class="recipe-filters"
    | Order by
    ul class="recipe-filter-list"
      li ng-repeat="type in recipes.config.orderTypes"
        a class="recipe-filter" ng-class="{ 'is-active': type.key == recipes.status.orderedBy }" ng-click="orderBy(type.key)"
          | {{ type.name }}

  div class="recipe-search" ng-submit="search()"
    form name="form"
      div class="search-input-container"
        input type="text" placeholder="Search" class="search-input" autocorrect="false" autocapitalize="off" ng-model="recipes.query"

section class="recipe-tiles"
  div class="recipe-tile" ng-repeat="recipe in recipes.items"
    a class="recipe-tile__link" ng-click="open(recipe)"
      div class="recipe-tile__aside"
        img class="recipe-tile__image" ng-src="{{ recipe.imageUrls.original }}"
      div class="recipe-tile__body"
        h2 class="recipe-tile__title" {{ recipe.name }}
        p class="recipe-tile__meta"
          span class="recipe-tile__meta-item"
            i class="fa fa-bookmark"
            | {{ recipe.bookmarks_count || 0 }}
          span class="recipe-tile__meta-item"
            i class="fa fa-eye"
            | {{ recipe.visitsCount || 0 }}
        / strong ng-show="recipe.status.bookmarked" Bookmarked
        / p ng-bind-html="recipe.description"
