h1 Login

form name="form" ng-submit="login()"
  input type="email" placeholder="E-mail" ng-model="currentUser.data.email"
  input type="password" placeholder="Password" ng-model="currentUser.data.password"

  button type="submit" Submit
